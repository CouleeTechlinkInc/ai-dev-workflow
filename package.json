{
  "name": "claude-docker-action",
  "version": "1.0.0",
  "private": true,
  "description": "Docker-based Claude Code action for GitHub workflows",
  "scripts": {
    "build": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@modelcontextprotocol/sdk": "^0.6.0",
    "@octokit/rest": "^20.0.0",
    "@octokit/types": "^12.0.0",
    "node-fetch": "^3.3.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "^1.2.12",
    "@types/node": "^20.0.0",
    "@types/node-fetch": "^2.6.11",
    "prettier": "3.5.3",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}